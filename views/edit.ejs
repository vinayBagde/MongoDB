<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your chat</title>
  </head>
  <body>
    <p>Edit your chat: <%= chat._id %></p>
    <p>
      This chat was sent form <b><%= chat.from %> </b>to <b><%= chat.to %></b>
    </p>

    <form action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <textarea name="msg" id="" rows="5" cols="50"><%=chat.msg %></textarea>
      <br />
      <button>Edit</button>
    </form>
  </body>
</html>
